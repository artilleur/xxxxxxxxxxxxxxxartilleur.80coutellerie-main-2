{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

    <table class="table">
    
        <tbody>
        <div class="container">
		<div class="row">
			<div class="col-12  text-center ">
            <tr>
                <th>Id</th>
            
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ produit.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ produit.image }}</td>
            </tr>
            <tr>
            <img style='max-width: 500px;' src='{{ vich_uploader_asset(produit, 'imageFile')}}'>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_article_index') }}">back to list</a>

    <a href="{{ path('app_article_edit', {'id': produit.id}) }}">edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
